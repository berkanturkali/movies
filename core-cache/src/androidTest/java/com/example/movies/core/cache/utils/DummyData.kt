package com.example.movies.core.cache.utils

import com.example.movies.core.cache.model.favorites.MovieEntity

object DummyData {

    val movieEntity = MovieEntity(
        id = 1,
        name = "Name",
        image = null
    )
}